<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Currency
    |--------------------------------------------------------------------------
    |
    | No guest-currency cookie configuration. Guest currency is now transported
    | via the X-Currency request header (Frontend-owned, Backend-validated).
    | The effective currency is resolved centrally in CurrencyService.
    |
    */
];
